type Result = variant { Ok : text; Err : text };

service : {
    get_erc20_redeem_signature: (
        token: text,
        amount: nat,
        receiver: text,
        tx_hash: text,
        nonce: nat,
        chain_id: nat,
        is_wrapped: bool,
        network: text
    ) -> (Result);
    eth_address: () -> (Result);
}
